name: Refresh TPE flights

on:
  schedule:
    # 每 30 分鐘（UTC），與主排程錯開 5 分鐘
    - cron: "5,35 * * * *"
  workflow_dispatch:

permissions:
  contents: write

env:
  TPE_SKIP_ODP: "1"
  TPE_SKIP_FLIGHTS_MERGE: "1"
  TPE_TIMEOUT: "60"
  TPE_CONNECT_TIMEOUT: "30"
  TPE_FETCH_RETRIES: "1"
  TDX_CLIENT_ID: ${{ secrets.TDX_CLIENT_ID }}
  TDX_CLIENT_SECRET: ${{ secrets.TDX_CLIENT_SECRET }}

jobs:
  refresh-tpe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch TPE snapshot
        run: python scripts/fetch_tpe_snapshot.py

      - name: Commit and push if changed
        id: commit
        run: |
          # 只提交桃園快照與機場對照；flights.json 由主排程合併，避免雙寫衝突
          git add data/tpe-flights.json data/airport-iata-zh.json public/data/airport-iata-zh.json
          bash .github/scripts/commit-push-retry.sh "chore: refresh TPE flight data"

      - name: Purge jsDelivr cache
        if: steps.commit.outputs.changed == 'true'
        run: |
          curl -fsS "https://purge.jsdelivr.net/gh/oURFo/typhoon-monitor@main/data/tpe-flights.json" || true
